layout:
    actions:
        - '@add':
              blockType: block
              parentId: page_header
              id: norsys_banner
              prepend: true
              options:
                  vars:
                      messages: '=data["norsys_banner.provider"].getActiveMessages()'
        
        -   '@setBlockTheme':
                themes: 'banner.html.twig'
